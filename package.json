{
    "name": "inspector-app-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build"
    },
    "engines": {
        "node": ">=16"
    }
}